///getAryOfInstancesInTriangle(obj, x1, y1, x2, y2, x3, y3)
var arr, n;
n = 0;
arr[0] = noone;
with (argument[0]) {
   if (point_in_triangle(x, y, argument[1], argument[2], argument[3], argument[4], argument[5], argument[6])) {
      arr[n++] = id;
   }
}
return arr;
