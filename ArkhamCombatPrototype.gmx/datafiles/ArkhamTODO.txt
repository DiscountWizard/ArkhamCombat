TODO:

- Factor out controls to an input listener object
	- Input listener tranlsates controls to game actions and data for game to use.
	- Ex: Based on controller and settings the X button, or the E key, or a mouse press will trigger ACTION4.
- Set speed based on framerate instead of hard coded pixels-per-update
- Add controller input